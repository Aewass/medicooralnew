---
interface Props {
  title: string;
  description?: string;
  image?: string;
  article?: boolean;
}

const { 
  title, 
  description = "MedicoOral - Stomatološka ordinacija u Bihaću. Pružamo vrhunske stomatološke usluge uz moderne tehnologije i stručni tim.",
  image = "/og-image.jpg",
  article = false
} = Astro.props;

const currentLang = Astro.url.pathname.split('/')[1] || 'bs';

// Safe URL construction
const siteUrl = 'https://medicooral.com';
const currentPath = Astro.url.pathname;
const canonicalURL = new URL(currentPath, siteUrl);
const alternateURLbs = new URL(currentPath.replace(/^\/en/, '') || '/', siteUrl);
const alternateURLen = new URL('/en' + currentPath.replace(/^\/bs/, ''), siteUrl);
const ogImage = new URL(image.startsWith('http') ? image : image, siteUrl);
---

<!doctype html>
<html lang={currentLang} class="scroll-smooth font-secondary">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    
    <!-- Primary Meta Tags -->
    <title>{title} | MedicoOral</title>
    <meta name="title" content={`${title} | MedicoOral`} />
    <meta name="description" content={description} />
    
    <!-- Canonical and Alternate Languages -->
    <link rel="canonical" href={canonicalURL} />
    <link rel="alternate" hreflang="bs" href={alternateURLbs} />
    <link rel="alternate" hreflang="en" href={alternateURLen} />
    <link rel="alternate" hreflang="x-default" href={alternateURLbs} />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content={article ? 'article' : 'website'} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={`${title} | MedicoOral`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:locale" content={currentLang === 'bs' ? 'bs_BA' : 'en_US'} />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={`${title} | MedicoOral`} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={ogImage} />
    
    <!-- Favicons -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    
    <!-- Additional Meta Tags -->
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#ffffff" />
    <meta name="robots" content="index, follow" />
    <meta name="author" content="MedicoOral" />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  /* Add any global styles here */
  section {
    @apply relative w-full;
  }

  /* Fix for background color overflow */
  section::before {
    content: '';
    @apply absolute inset-0 w-screen -z-10;
  }

  /* Apply background colors to specific sections */
  section[data-bg="secondary"]::before {
    @apply bg-[#1199ee];
  }

  section[data-bg="card"]::before {
    @apply bg-[#e8f7ff];
  }

  /* Add after your existing styles */
  section[data-pattern="grid"] {
    position: relative;
  }

  section[data-pattern="grid"]::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(to right, #1199ee10 1px, transparent 1px),
      linear-gradient(to bottom, #1199ee10 1px, transparent 1px);
    background-size: 24px 24px;
    pointer-events: none;
  }

  section[data-pattern="dots"] {
    position: relative;
  }

  section[data-pattern="dots"]::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image: radial-gradient(#1199ee10 1px, transparent 1px);
    background-size: 24px 24px;
    pointer-events: none;
  }

  .floating {
    animation: floating 3s ease-in-out infinite;
  }

  @keyframes floating {
    0% { transform: translate(0, 0); }
    50% { transform: translate(0, 10px); }
    100% { transform: translate(0, 0); }
  }
</style> 